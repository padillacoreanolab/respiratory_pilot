[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "respiratory_pilot"
version = "0.1.0"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "pandas"
]

[tool.setuptools]
packages = ["src.data_processing", "src.plotting"]
